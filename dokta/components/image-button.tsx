
import { View, Text } from 'react-native';

const ImageButton = ({

}: {
    icon: string;
    onClick: () => void;
}) => {
  return (
    <View>
      <Text>ImageButton</Text>
    </View>
  )
}

export default ImageButton;